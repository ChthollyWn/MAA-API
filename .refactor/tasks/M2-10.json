{
  "id": "M2-10",
  "milestone": "M2",
  "title": "仓储层（四）：AgentSession/AgentMessage/Audit + Confirmation（审计裁剪与终态不可变）",
  "depends": [
    "M2-07"
  ],
  "reads": [
    "docs/12-实施计划与里程碑.md#m2--数据层l",
    "docs/04-数据模型与持久化.md#57-agent_session--内置-agent-会话",
    "docs/04-数据模型与持久化.md#58-agent_message--会话消息与工具调用轨迹",
    "docs/04-数据模型与持久化.md#59-agent_audit--全量工具调用审计",
    "docs/04-数据模型与持久化.md#510-confirmation--人工确认请求",
    "docs/04-数据模型与持久化.md#9-仓储层",
    "tests/fixtures/db_probe_findings.md",
    "tests/fixtures/db_probe_result.json"
  ],
  "constraints": [
    "入库前必须裁剪 `arguments` 与 `result_summary`：截图工具的返回值是 base64 字符串，约定任何超过 1 KB 的字符串值替换为 `{\"__truncated__\": true, \"len\": 412300, \"ref\": \"screenshot_id\"}`；`result_summary` 上限 2 KB（docs/04 §5.9）。",
    "`confirmation_id` 与 `authorized_by_id` 互斥：一次调用要么走了逐次确认（前者有值），要么凭会话级授权免确认执行（后者有值），要么本来就不需要确认（两者皆空）（docs/04 §5.9）。",
    "`confirmation.payload` 存的是待执行动作的完整参数，不是引用。这样批准后不依赖发起方进程还活着；服务重启后可以扫出 `PENDING` 记录并一律置 `EXPIRED`（docs/04 §5.10）。",
    "状态流转只允许 `PENDING → APPROVED / REJECTED / EXPIRED`，终态不可再变，重复批准由 `UPDATE ... WHERE status='pending'` 的影响行数判定；`expire_overdue()` 返回被置为过期的 id 列表而不是条数，因为服务层要据此广播并唤醒阻塞调用（docs/04 §5.10/§9）。",
    "`agent_session.atomic_grant_*` 两个字段承载会话级原子操作授权，但授权判定、窗口计算与撤销语义归 M11；本卡只做持久化与查询，不实现 `PolicyEngine`/确认编排（docs/04 §5.7）。"
  ],
  "deliverables": [
    "maa_api/db/repositories/agent.py",
    "maa_api/db/repositories/audit.py",
    "tests/db/test_repositories_agent_audit.py"
  ],
  "verify": [
    "test -f maa_api/db/repositories/agent.py -a -f maa_api/db/repositories/audit.py",
    ".venv/bin/python -c \"from maa_api.db.repositories.agent import AgentSessionRepository, AgentMessageRepository; from maa_api.db.repositories.audit import AuditRepository, ConfirmationRepository; need={'AgentSessionRepository':['create','get','list_recent','update_grant','clear_grant'],'AgentMessageRepository':['create','list_by_session'],'AuditRepository':['create','get','list'],'ConfirmationRepository':['create','get','list_pending','resolve','expire_overdue']}; cls={'AgentSessionRepository':AgentSessionRepository,'AgentMessageRepository':AgentMessageRepository,'AuditRepository':AuditRepository,'ConfirmationRepository':ConfirmationRepository}; bad=[(c,n) for c,ms in need.items() for n in ms if not callable(getattr(cls[c],n,None))]; assert not bad, bad; print('REPO CONTRACT OK')\"",
    ".venv/bin/python - <<'PY'\nimport asyncio, pathlib, tempfile\nfrom datetime import timedelta\nfrom sqlalchemy.ext.asyncio import async_sessionmaker, create_async_engine\nfrom sqlalchemy.pool import NullPool\nfrom sqlmodel import SQLModel\nimport maa_api.db.models  # noqa: F401\nfrom maa_api.db.models import AgentAudit, AgentMessage, AgentSession, Confirmation, utcnow\nfrom maa_api.db.repositories.agent import AgentMessageRepository, AgentSessionRepository\nfrom maa_api.db.repositories.audit import AuditRepository, ConfirmationRepository\nfrom maa_api.domain.enums import (AgentRole, AgentSessionStatus, AuditStatus, CallerType,\n                                  ConfirmationStatus, RiskLevel)\n\nd = pathlib.Path(tempfile.mkdtemp())\neng = create_async_engine(f\"sqlite+aiosqlite:///{d / 't.db'}\", poolclass=NullPool)\nsf = async_sessionmaker(eng, expire_on_commit=False)\nnow = utcnow()\n\n\nasync def main():\n    async with eng.begin() as conn:\n        await conn.run_sync(SQLModel.metadata.create_all)\n    async with sf() as s:\n        arepo, mrepo = AgentSessionRepository(s), AgentMessageRepository(s)\n        audit, conf = AuditRepository(s), ConfirmationRepository(s)\n\n        big = \"x\" * 5000\n        row = await audit.create(AgentAudit(caller=CallerType.MCP, tool_name=\"screenshot\",\n                                            arguments={\"image\": big, \"small\": \"ok\"},\n                                            result_summary=\"y\" * 5000, status=AuditStatus.SUCCESS,\n                                            risk_level=RiskLevel.NONE))\n        assert row.arguments[\"image\"][\"__truncated__\"] is True, row.arguments[\"image\"]\n        assert row.arguments[\"image\"][\"len\"] == 5000, row.arguments[\"image\"]\n        assert row.arguments[\"small\"] == \"ok\", row.arguments\n        assert len(row.result_summary) <= 2048, len(row.result_summary)\n\n        ses = await arepo.create(AgentSession(model=\"gpt-x\", status=AgentSessionStatus.ACTIVE))\n        await mrepo.create(AgentMessage(session_id=ses.id, seq=0, role=AgentRole.USER, content=\"hi\"))\n        await mrepo.create(AgentMessage(session_id=ses.id, seq=1, role=AgentRole.ASSISTANT, content=\"yo\"))\n        await s.commit()\n        msgs = await mrepo.list_by_session(ses.id)\n        assert [m.seq for m in msgs] == [0, 1], [m.seq for m in msgs]\n\n        expired = await conf.create(Confirmation(action=\"submit_pipeline\", risk_level=RiskLevel.CONSUME,\n                                                reason=\"消耗类\", payload={\"a\": 1}, requested_by=CallerType.MCP,\n                                                expires_at=now - timedelta(seconds=1)))\n        await s.commit()\n        assert await conf.expire_overdue(now) == [expired.id]\n        await s.commit()\n        assert await conf.expire_overdue(now) == []\n        assert await conf.resolve(expired.id, ConfirmationStatus.APPROVED, resolved_by=\"web\") is False, \"终态不可再变\"\n        pending = await conf.create(Confirmation(action=\"click\", risk_level=RiskLevel.NONE, reason=\"原子操作\",\n                                                payload={}, requested_by=CallerType.INTERNAL,\n                                                expires_at=now + timedelta(minutes=5)))\n        await s.commit()\n        assert [c.id for c in await conf.list_pending()] == [pending.id]\n        assert await conf.resolve(pending.id, ConfirmationStatus.APPROVED, resolved_by=\"web\") is True\n        assert await conf.resolve(pending.id, ConfirmationStatus.REJECTED, resolved_by=\"web\") is False\n        await s.commit()\n        got = await conf.get(pending.id)\n        assert got.status == ConfirmationStatus.APPROVED and got.resolved_at is not None\n    await eng.dispose()\n    print(\"AGENT/AUDIT REPO OK\")\n\n\nasyncio.run(main())\nPY",
    ".venv/bin/python -m pytest -q tests/db/test_repositories_agent_audit.py",
    ".venv/bin/python -m pytest -q"
  ],
  "prompt_extra": "【接口契约（M2-11/M12 与 M11 按这个用）】\n`AgentSessionRepository`：`create`、`get`、`list_recent(limit=20)`（按 `last_message_at` DESC，NULL 排最后）、`update_grant(session_id, *, confirmation_id, expires_at)`、`clear_grant(session_id)`（把两个字段一起置空，不是把时间改成过去，见 docs/04 §5.7）。\n`AgentMessageRepository`：`create(message)`、`list_by_session(session_id)`（按 seq 升序）。\n`AuditRepository`：`create(audit) -> AgentAudit`（在仓储内做 §5.9 的裁剪：递归处理 JSON 里的字符串值，>1KB 的替换为 `{\"__truncated__\": true, \"len\": n}`；`result_summary` 截断到 2048 字符）、`get(id)`、`list(*, caller=None, tool_name=None, page=1, size=20)`。\n`ConfirmationRepository`：`create`、`get`、`list_pending`、`resolve(id, status, *, resolved_by, reason=None) -> bool`、`expire_overdue(now) -> list[str]`。\n\n【实现要点】`resolve`/`expire_overdue` 用 `update(...).where(id == ?, status == PENDING)` + `rowcount`；`expire_overdue` 批量置 EXPIRED 后把 id 列表查出来返回。裁剪函数建议独立成模块级函数便于测试；`arguments` 与 `result_summary` 都是 JSON/TEXT 可序列化值。\n\n【测试】复用 `tests/db/conftest.py` 的 fixture；同步测试 + `asyncio.run`。\n\n【环境事实（已实测，勿重复踩）】`python` 不在 PATH，一律用 `.venv/bin/python`（只有 `python3` 3.13.3）；本仓没有 pytest-asyncio，异步用例请用同步测试函数 + `asyncio.run(...)` 包住，不要新增 pytest 插件依赖；禁用 `import maa_api.main` 做自检（import 期会走 Updater + dlopen，约 2.5 分钟）。【收尾】逐条跑 verify 全绿后再提交；只 `git add` 本卡 deliverables（绝不 `git add -A`），commit message 用 `M2-XX: <标题>`，不 push、不切分支；不改动 `.refactor/` 下任何文件（ENVIRONMENT.md 是唯一例外：只追加实测事实、不改写既有条目、不 git add）。回执 200 字以内。",
  "status": "pending",
  "attempts": 0,
  "commit": null,
  "blocked_reason": null
}
