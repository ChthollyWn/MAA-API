{
  "milestone": "M8",
  "deliverable": "交付可离线构建、由 FastAPI 同源托管的移动优先 React 前端基座。用户可通过底部导航打开仪表盘与日志页；仪表盘展示服务/内核/设备状态、当前流水线和设备截图，日志页显示历史及实时日志；含 OpenAPI 同步类型、鉴权、WebSocket 重连/补发、深色/浅色主题、安全区适配和 PWA 静态外壳。",
  "acceptance": [
    ".venv/bin/python scripts/dump_openapi.py --check",
    "cd web && pnpm install --frozen-lockfile && pnpm gen:api:check && pnpm typecheck && pnpm test --run && pnpm build",
    ".venv/bin/python -m pytest -q",
    ".venv/bin/python scripts/frontend_smoke.py"
  ],
  "advisory": [
    "使用真实 iOS/Android 手机经 Tailscale Serve 打开服务，核对安全区、安装提示、截图、设备/内核状态和实时日志；推送通知由 M14 验收。若本机没有可用设备或 tailnet，记录证据，不以本地浏览器模拟结果替代真机结论。",
    "本机 Docker Engine 不可用：`docker buildx build --check -f Dockerfile .` 无法连接 Docker socket；提权重试仍报 daemon 未运行，已装 `/Applications/Docker.app` 启动时报缺可执行文件 `kLSNoExecutableErr`。容器文件由 pytest 静态契约覆盖；恢复 Docker Engine 后仍需实际构建镜像。"
  ],
  "_note": "M8 实现提交为 8823ef9；九张任务卡均由主控独立复核并运行卡面验证。Dockerfile/README 结构由离线测试覆盖，但本机 Docker Engine 不可用，无法进行实际镜像构建。已用已连接的 SM-G998B Android Chrome 经 ADB reverse 打开 production build；本地 mock API/图像/WS 驱动设备截图、三层状态和日志，验证安全上下文、PWA service worker、深链与移动布局。Tailscale Serve 未验证：本机 tailscale wrapper 指向缺失的 /Applications/Tailscale.app executable；真 MAA Core 数据未接入预览。硬验收全通过后再由主控提交验收台账并创建 v2-m8。"
}
