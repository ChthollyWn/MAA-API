{
  "milestone": "M4",
  "deliverable": "能用 `websocat` 之类的工具连上 WS 看到三路日志实时到达；日志可查历史、可过滤。",
  "acceptance": [
    ".venv/bin/python -m pytest -q",
    ".venv/bin/python scripts/log_ws_smoke.py"
  ],
  "advisory": [
    ".venv/bin/python scripts/log_ws_smoke.py --serve --port 8124",
    ".venv/bin/python scripts/log_ws_smoke.py --real-core"
  ],
  "_note": "acceptance 是硬门禁：全量 pytest 覆盖日志结构/背压/映射表/tailer/截图存储/WS 协议/历史查询各卡单测；log_ws_smoke.py 默认模式在隔离临时目录里用真实 app 装配（lifespan 起 LogHub）证明 docs/12 给 M4 定的「可交付状态」整条链路跑得通 —— FakeAsst + 真实 CoreSupervisor/CoreClient 驱动 task 源、临时 asst.log 驱动 core 源、真实 logging 驱动 service 源，三路经 WS 到达，并覆盖补发、订阅过滤、历史查询/导出/清理与截图附件引用，末行必须是 SMOKE OK。advisory 不进 tag 门禁：--serve 起真实 uvicorn 与本地端口（本机没装 websocat/websockets，脚本用标准库 socket 做握手），--real-core 需要真机与模拟器（127.0.0.1:5555），两者的失败可能来自端口占用、设备未启动等环境抖动。本文件与 M4-11 卡配套；两者缺一，M4 就不能算交付。"
}
