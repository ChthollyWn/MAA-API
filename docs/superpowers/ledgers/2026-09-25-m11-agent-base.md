# SDD ledger — plan: docs/superpowers/plans/2026-09-25-m11-agent-base.md

## Setup

- Base: `0e28ee2` (`v2-m10`), branch: `m11-agent-base`; working directly in the authorized checkout because project AGENTS.md prohibits worktrees.
- Plan: `docs/superpowers/plans/2026-09-25-m11-agent-base.md`; spec: docs/11 and docs/12, with README decisions and docs/13 §8 precedence.
- Preflight scan: Task 1 defines registry/policy interfaces consumed by Tasks 2–5; Task 2 owns structured callback statistics + status/pipeline tools; Tasks 3–4 write distinct tool modules and distinct test files under `maa_api/agent/tools/` and `tests/agent/`; Task 5 consumes those interfaces and owns API/router/repository/lifespan files; Task 6 consumes Task 5 REST/WS contracts and owns frontend feature files; Task 7 owns generated artifacts, smoke script, and docs, while Task 6 must not edit generated types. No same-file overlap remains after this boundary.
- Self-consistency: Task 1 tests its registry/policy contract; Tasks 2–4 test each separate tool group; Task 5 tests confirmation/session/audit REST behavior; Task 6 tests confirmation and audit UI; Task 7 verifies generated contracts and end-to-end smoke. All implementation tasks require tests first.

## Rulings I made

- Ruling: Use the current `m11-agent-base` checkout without a worktree — project AGENTS.md explicitly forbids worktrees; cost if wrong: isolation is weaker, so tasks are restricted to disjoint file sets.
- Ruling: Follow project AGENTS.md integration target `refactor/v2`, not docs/12 §4's older `dev` wording — the current task instructions prohibit dev integration; cost if wrong: any historic downstream workflow expecting dev would need a separate authorized integration.
- Ruling: Persist `StageDrops` and `SanityBeforeStage` in structured tables and aggregate from those records instead of scraping `LogEntry.content` — docs/06 requires independent structured statistics and the display text is lossy; cost if wrong: this adds a migration and callback persistence path that could have been deferred.
- Ruling: `agent_audit` rows transition once from `PENDING` to a terminal status — async confirmation requires a durable in-flight audit row and one stable `audit_id`; cost if wrong: this relaxes the existing repository's append-only assumption and needs docs/04 to state the permitted terminal update.
- Ruling: Persist REST idempotency in a separate 24-hour `agent_idempotency` table rather than add a permanent unique key to `agent_audit` — the audit table is long-lived and keys are only reserved for 24 hours; cost if wrong: adds one migration/table where a simpler audit-column design might have sufficed.
- User decision: Defer the real Copilot job runner; do not expose `run_copilot` as a tool or substitute a normal Fight call. Keep validated Copilot upload/listing and continue the other M11 tools.

## Task progress

- Task 1: complete (`bf0ef3c` plus fail-closed fixes `85284e1`, `ffa66b2`, `edada6e`); policy focused suite and scoped reviews clean.
- Task 2: complete (`8b68fd0`, partial-patch fix `7b77849`, reports/docs `4e6a6e7`, `6451fc1`, `3789f6a`); related suite 425 passed, patch-review suite 232 passed.
- Task 3: complete (`4b3be5f`); review clean. Non-blocking observation: raw screenshot returns full-resolution base64 without an output cap, permitted by spec.
- Task 4: complete (`35965c7`, report `aed0516`, fix `36a5a3e`, report `b65471d`); final focused suite 32 passed and scoped review found no blocking issue. Two malformed local Copilot samples are rejected; structural asset counts are recorded in the report.
- Task 5: base and fail-closed implementation committed (`013bbff`, `5a95ade`); final-review idempotency recovery correction implemented in the current worktree and under final verification. Main-agent focused recovery suite: 198 passed (latest final rerun includes migration and same-key response replay coverage).
- Task 6: complete in the current worktree; confirmation/audit UI, router, navigation and WebSocket recovery tests passed. Three reviewer findings were fixed with red/green coverage.
- Task 7: complete in the current worktree; M11 spec, docs, agent smoke and generated contracts checked. Main-agent hard verification recorded below.

## Task 1 brief review

- Contract: `ToolDefinition(name, group, risk, description, params_model, handler)`; `ToolContext(caller, session_id, request_id, request, db_session)`; `ToolRegistry.register/get/list/validate/execute`; `PolicyEngine.evaluate(definition, arguments, context) -> PolicyDecision`.
- Task 1 owns `registry.py`, `policy.py`, and a new `tests/agent/test_registry_policy.py`; no later task edits those production files.
- Task 2 now owns the structured stats persistence producer and status/pipeline tool modules; downstream `get_drop_stats` consumes the repository/service it adds.

## Main-agent final verification

- `.venv/bin/python -m pytest -q`: final isolated rerun exit 0; an earlier concurrent run hit the existing tight `CoreSupervisor` backoff timing assertion (0.1516s vs 0.15s), and the isolated test plus full-suite rerun passed. Starlette/httpx deprecation warnings remain.
- `.venv/bin/python scripts/agent_smoke.py`: `SMOKE OK`.
- `.venv/bin/python scripts/api_smoke.py`: `SMOKE OK`.
- `.venv/bin/python scripts/dump_openapi.py --check`: current.
- `cd web && pnpm install --frozen-lockfile && pnpm gen:api:check && pnpm typecheck && pnpm test --run && pnpm build`: exit 0; 15 files / 117 tests. pnpm emitted a non-fatal package-manager metadata fetch warning.
- `.venv/bin/python scripts/frontend_smoke.py`: `SMOKE OK`.
- `.venv/bin/python scripts/generate_api_guide.py --check`: current.
- Real MaaCore/Android and live visual-model checks were not executed: ADB daemon startup is denied by this environment and no LLM endpoint is configured. They remain supplemental; see docs/13 §8.3.

## Additional final-review ruling

- **Idempotency crash recovery:** persist request mode with the 24-hour reservation; async first response is committed before starting its worker; sync response snapshot is committed with the audit terminal state. Startup recovery returns a stable error with `audit_id` when an exact successful body cannot be recovered, instead of executing again or leaving a permanent `in_progress`. A secondary response-write failure never deletes a linked reservation.
